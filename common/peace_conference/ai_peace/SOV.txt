# Scripted AI desire for peace actions.
# See documentation.info file for more detailed scripting information.

peace_ai_desires = {

     # Russia really wants that Königsberg place for themselves, don't they?
    RUS_wants_konigsberg = {
        peace_action_type = take_states
        enable = {
            tag = RUS  # Negotiatior is RUSiet union
            FROM = { tag = ROOT }  # And the negotiator is also taking for themselves
            FROM.FROM.FROM = { state = 763 }  # Königsberg
        }

        ai_desire = 50  # 50 percent higher desire
    }

    # If Lithuania is not a Russia Puppet, the Russia want Memel too
    RUS_wants_memel = {
        peace_action_type = take_states
        enable = {
            tag = RUS  # Negotiatior is RUSiet union
            NOT = { LIT = { is_subject_of = ROOT } }
            FROM = { tag = ROOT }  # And the negotiator is also taking for themselves
            FROM.FROM.FROM = { state = 188 }  # Memel
        }

        ai_desire = 50  # 50 percent higher desire
    }
     # If Lithuania is a RUSiet Puppet, the RUSiets want Memel for it
    RUS_wants_memel = {
        peace_action_type = take_states
        enable = {
            tag = RUS  # Negotiatior is RUSiet union
            LIT = { pc_is_puppeted_by = ROOT }
            FROM = { tag = LIT }  # And the negotiator is also taking for themselves
            FROM.FROM.FROM = { state = 188 }  # Memel
        }

        ai_desire = 50  # 50 percent higher desire
    }


    RUS_czech_puppet_histo = {
        peace_action_type = puppet
        enable = {
            is_historical_focus_on = yes
            ROOT.FROM = { tag = CZE }
            ROOT.FROM.FROM.FROM = { is_core_of = CZE }
            tag = RUS
        }
        ai_desire = 150
    }


    RUS_claims_histo = {
        peace_action_type = take_state
        enable = {
            is_historical_focus_on = yes
            ROOT.FROM.FROM.FROM = {
                OR = {
                    state = 73
                    state = 763
                    state = 5
                }
            }
            ROOT.FROM = { tag = RUS }
        }
        ai_desire = 400
    }


    RUS_leave_austria_alone = {
        peace_action_type = { take_states puppet liberate force_government }
        enable = {
            is_historical_focus_on = yes
            ROOT = { tag = RUS }
            ROOT.FROM.FROM.FROM = { is_core_of = AUS }
        }
        ai_desire = -50
    }


    # Create a Korean puppet in North Korea.
    RUS_puppet_n_korea = {
        peace_action_type = { puppet }
        enable = {
            FROM.FROM.FROM = {
                state = 527
            }
            FROM = {
                original_tag = KOR
            }
            ROOT = {
                original_tag = RUS
                has_government = communism
            }
        }
        ai_desire = 75
    }


    # The 38th parallell - don't take South Korea if the US are on the same side
    RUS_not_puppet_s_korea = {
        peace_action_type = { puppet take_states liberate force_government}
        enable = {
            is_historical_focus_on = yes
            FROM.FROM.FROM = {
                state = 525
            }
            FROM = {
                original_tag = KOR
            }
            ROOT = {
                original_tag = RUS
                has_government = communism
            }
            USA = {
                OR = {
                    has_war_with = JAP
                    has_war_with = KOR
                }
                NOT = { has_war_with = ROOT }
            }
        }
        ai_desire = -100
    }
}
